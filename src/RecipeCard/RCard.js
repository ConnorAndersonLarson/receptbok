import './RCard.scss';
import { useState } from 'react';

function Rcard() {
  const [photo, setPhoto] = useState('Inga/0.jpg');

  return(
    <div class="recipeBox">
    </div>
  );
}

export default Rcard;
